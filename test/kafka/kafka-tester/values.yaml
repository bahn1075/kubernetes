# Default values for kafka-tester
replicaCount: 1

image:
  repository: confluentinc/cp-kafka
  tag: "7.4.0"
  pullPolicy: IfNotPresent

kafka:
  bootstrapServers: "my-cluster-kafka-bootstrap:9092"

producer:
  enabled: true
  topic: test-topic
  message: "Hello Kafka"

consumer:
  enabled: true
  topic: test-topic

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
